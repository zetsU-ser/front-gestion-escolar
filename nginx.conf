server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Esta línea es crucial para que funcione React Router (SPA)
        try_files $uri $uri/ /index.html;
    }
}
