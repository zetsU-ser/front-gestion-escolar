import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0D47A1', // Azul
    },
    secondary: {
      main: '#B71C1C', // Rojo
    },
  },
});

export default theme;

